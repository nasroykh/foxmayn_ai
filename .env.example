# ============================================================
# Environment Variables — Single source of truth for all apps
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# Local dev: loaded by dotenv (API/DB/LLM/Qdrant) and Vite (frontend)
# Docker:    Docker Compose reads .env for ${VAR} interpolation.
#            Some vars (NODE_ENV, HOST, DB_HOST, DB_URL) are
#            overridden inside docker-compose.yml directly.
# ============================================================

# Environment
NODE_ENV=development

# Super Admin Configuration
SUPER_ADMIN_EMAIL=superadmin@gmail.com
SUPER_ADMIN_PASSWORD=azertyuiop

# Server Configuration
PORT=33450
HOST=localhost
API_V1_PREFIX=/api/v1

# App URLs
APP_URL=http://localhost:33460
APP_URL_DEV=http://localhost:33460

# Frontend
VITE_API_URL=http://localhost:33450/api/v1

# Auth
TOKEN_SECRET_KEY=OImQHqNhtw37QWYGzglZ9ZuPXdczmmA@8TIP
TOKEN_ENCRYPTION_KEY="HSt+a71Gnq9DkkgGeKSnWJZJ9/yDa85AIjdJHFEN74U="
BETTER_AUTH_SECRET=0Tu0Vx9su71KeT8Dbkm7MyekMLGf7mrc

# Database
DB_URL="postgresql://foxmayn_ai_user:foxmayn_ai_password@localhost:5432/foxmayn_ai_db?sslmode=disable"
DB_USER="foxmayn_ai_user"
DB_PASSWORD="foxmayn_ai_password"
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="foxmayn_ai_db"
DB_SSL_MODE="disable"

# SMTP
SMTP_HOST=smtp.domain.com
SMTP_PORT=587
SMTP_USER=test@domain.com
SMTP_PASSWORD=password
SMTP_FROM=test@domain.com

# Redis
REDIS_URL=redis://localhost:6379

# LLM
OPENROUTER_API_KEY=sk-or-...
OPENAI_API_KEY=sk-...

# Qdrant
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=documents
